<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EBooking" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@drawable/background_image</item>
    </style>
    <style name="Base.Theme.ClassicBackground" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/background_solid</item>
    </style>
    <style name="Base.Theme.Background" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/background</item>
    </style>

    <style name="Theme.EBooking" parent="Base.Theme.EBooking" />
    <style name="Theme.ClassicBackground" parent="Base.Theme.ClassicBackground"/>
    <style name="Theme.Background" parent="Base.Theme.Background"/>
</resources>