<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ebooking"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ebooking"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Background"
        tools:targetApi="31">

        <activity
            android:name=".activity.login.Login"
            android:exported="true"
            android:theme="@style/Base.Theme.EBooking">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.register.Register"
            android:exported="true"
            android:theme="@style/Base.Theme.EBooking">
        </activity>
        <activity
            android:name=".activity.homepage.Homepage"
            android:exported="true"
            android:theme="@style/Base.Theme.ClassicBackground">
        </activity>
        <activity
            android:name=".activity.filteredrooms.FilteredRooms"
            android:exported="true"
            android:theme="@style/Base.Theme.ClassicBackground">
        </activity>
        <activity android:name=".activity.filter.Filter"
            android:exported="true"
            android:theme="@style/Base.Theme.Background">
        </activity>
        <activity android:name=".activity.selectedlodge.SelectedLodge"
            android:exported="true"
            android:theme="@style/Base.Theme.Background">
        </activity>
    </application>

</manifest>